# Connectly client

Install and run:

```bash
npm install
npm run dev
```

The client expects the API at `http://localhost:3001` by default. Copy `.env.example` to `.env` only when a different API origin is required.
